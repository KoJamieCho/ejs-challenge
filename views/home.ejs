<%- include('partials/header'); -%>
    <h1>Home</h1>
    <p> <%=startingContent%> </p>    
    <% posts.forEach(function(val) { %>
        <h1> <%= val.title %></h1>
        <p> <%= val.content %> </p>    
    <% }); %>
    
    <% console.log(posts); %>

<%- include('partials/footer'); -%>
